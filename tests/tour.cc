#include <cmath>
#include <iostream>
#include <assert.h>
#include <Tour.h>

using namespace std;

int main() {
	/*Tour tour;

	assert(score.get_score() == 0 && score.get_kills() == 0);

	score.set_kills(10);
	score.set_score(250);

	assert(score.get_score() == 250 && score.get_kills() == 10);*/

	return 0;
}
